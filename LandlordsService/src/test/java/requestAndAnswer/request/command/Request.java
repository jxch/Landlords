package requestAndAnswer.request.command;

public enum Request {
    login, //登陆
    registered, //注册
    forgetPassword, //忘记密码
    myInfo, //我的信息
    roomOutRequest, //房间外请求
    roomInRequest, //房间内请求
}
